
/**
 * The toolbar display contextual action.
 */
var Toolbar = function(parent){
    
    // The toolbar panel.
    this.panel = parent.appendElement({"tag":"div", "class":"toolbar"})

    return this
}

